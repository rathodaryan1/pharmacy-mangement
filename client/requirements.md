## Packages
recharts | Beautiful, responsive charts for the dashboard, sales, and analytics pages
lucide-react | Standardized icon set for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
- Building a frontend-only prototype using purely mock data.
- The design implements a specific Emerald Green / Lime Green theme with a soft light gray background.
- Routing is handled via wouter.
